# 交接文档模板（前后端 handoff）

> 用途：一个会话做完某块功能后，写给另一个会话（或人）的交接说明。
> 命名：`docs/handoff-YYYY-MM-DD-<前后端>-<主题>.md`（如 `handoff-2026-08-20-backend-评分接口.md`）。
> 参考 tzu 的 `backend-handoff.md` / `frontend-handoff-kimi.md`，按需删减小节。

---

# 给后端的交接信息（前端 <功能> 进度）

> 日期：YYYY-MM-DD · 写给正在做 `backend/` 的 opencode 会话
> 目的：前端已用 mock 做出 <功能>，后端按此契约实现真接口后，前端再切真数据。

## 1. 当前状态

- 页面：`/xxx` 做了什么
- 组件：`frontend/app/...` 清单
- 数据源：`useXxxData.ts`（纯 mock / 已切真接口）

## 2. 后端待办清单

按 `docs/API契约.md` 顺序列出未完成端点：

1. `POST /api/xxx` — 说明 + 校验规则（字段、长度、必填）
2. `GET /api/xxx` — 说明 + 分页口径

契约没定义、但前端已做出、需后端**决策**的点：

- 某个交互按钮后端做不做？建议接口签名。

## 3. 前端当前类型定义（mock）

```ts
interface Xxx { /* 字段 */ }
```

## 4. 切真数据时前端要改的点

| 现在 mock | 契约 | 影响 |
|-----------|------|------|
| `author: string` | `author: {...}` | 取值方式变 |

## 5. 给后端的问题（需回复确认）

1. ……

## 6. 后端完成状态（后端实现后回填）

- 已实现接口、变更字段、测试覆盖数。

---

# 给前端的交接信息（后端 <功能> 进度）

> 日期：YYYY-MM-DD · 写给正在做 `frontend/` 的 opencode 会话
> 目的：后端已按契约实现 <接口>，前端切真数据 + 处理新增字段。

## 0. 开发约定

- dev server 由用户自己启动；改动共享文件前先读现状；提交用 conventional commits + 中文描述。

## 1. 已实现接口

| 方法 | 路径 | 说明 | 响应要点 |
|------|------|------|----------|
| GET | /api/xxx | ... | ... |

## 2. 新增/变更字段（相对契约）

## 3. 前端待办清单

1. `useXxxApi.ts` 加方法
2. 类型对齐（字段名差异）
3. 降级逻辑移除（mock 已可删）

## 4. 验证命令

```bash
cd backend && uv run pytest
cd frontend && pnpm build
```

## 5. 给前端的问题
