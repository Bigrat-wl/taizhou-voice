---
tags: [语音识别, 方言, 开发清单, 追踪]
created: 2026-08-19
status: growing
---

# 泰州方言通 开发清单

> 细化任务清单，用于执行追踪。时间目标：21 号前完成比赛演示（今天 19 号晚）。

## 当前进度

| 已完成 | 状态 |
|---|---|
| 后端骨架 + ASR 识别 + CORS | ✅ |
| 前端骨架（Nuxt4 + Tailwind + proxy） | ✅（待提交 git） |

## 任务清单

### 第一批：数据层（阻塞后续，先做）

| # | 任务 | 内容 | 验证 |
|---|---|---|---|
| 1 | SQLAlchemy 模型 | 建 `sentences` / `users` / `recordings` / `translations` 四张表 | 建表成功 |
| 2 | 句子种子数据 | 从 `hailing_asr/data/metadata.csv` 导入 27 句 | 库里查到 27 条 |

### 第二批：后端 API（挑战赛闭环）

| # | 任务 | 内容 | 验证 |
|---|---|---|---|
| 3 | 句子接口 | `GET /api/sentences?n=5` 随机取 N 句 | curl 返回句子列表 |
| 4 | 评分服务 | 复用 ASR 识别 → 识别文本 ↔ 参考文本相似度打分 | 单测通过 |
| 5 | 评分接口 | `POST /api/score` 上传音频 → 存录音 + 返回分数/等级 | curl 返回分数 |
| 6 | 排行榜接口 | `GET /api/leaderboard` 按分数实时查询 | curl 返回排名 |

### 第三批：前端页面（核心演示）

| # | 任务 | 内容 | 验证 |
|---|---|---|---|
| 7 | 首页导航 | 四功能入口 | 页面渲染 |
| 8 | 挑战赛页 | 刷句子 → 录音 → 提交 → 分数/等级 | 走通流程 |
| 9 | 排行榜页 | 展示排名 | 显示排名 |
| 10 | 转写页 | 复用 `/api/asr`，录音/上传 → 文本 | 出文本 |

### 第四批：次优先 + 联调

| # | 任务 | 内容 | 验证 |
|---|---|---|---|
| 11 | TTS 封装 + 页面 | CosyVoice2 封装 + 文本→方言语音页 | 游戏本验证 |
| 12 | 音频互转页 | 方言语音 ↔ 普通话音频 | 游戏本验证 |
| 13 | 前后端联调 | 含 webm 解码验证（= 调整项 #3） | 端到端走通 |
| 14 | 游戏本部署 | 全功能 | 全功能跑通 |

## 依赖关系

```
第一批(数据层) → 第二批(API) → 第三批(前端页面) → 第四批(联调/部署)
     │              │
     └── 挑战赛评分/排行榜依赖句子表和录音表
                    └── TTS/互转(11/12) 可并行，游戏本验证
```

## 与「后端 review 调整项」的关联

- 开发清单 #13（联调）触发 调整项 #3（webm 解码验证）
- 开发清单 #14（部署）触发 调整项 #2/#4/#5（GPU 方案/模型路径/GPU 实测）
