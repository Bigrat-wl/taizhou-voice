---
tags: [语音识别, 方言, 进度追踪]
created: 2026-08-19
status: growing
---

# 泰州方言通 开发进度

> 最后更新：2026-08-19
> 时间目标：21 号前完成比赛演示。
> ⚠️ 数据模型已升 v2（五张表 + 邮箱登录 + 点赞 + 中国时区），任务清单据此重构，见 `docs/handoff-2026-08-19-数据模型v2同步.md`。

## 进度总览

- **已完成**：2 / 19（#2 种子、#3 句子接口）
- **待重验**：#1 模型（已升 v2 五张表，需重新建表验证）
- **下一步**：#4 用户系统（注册/登录 + 鉴权）

## 任务进度表

### 第一批：数据层

| # | 任务 | 验证 | 状态 | 完成时间 | 备注 |
|---|---|---|---|---|---|
| 1 | SQLAlchemy 模型（5 张表，邮箱登录，上海时区） | 建表成功 | 🟡 待重验 | 2026-08-19 | 原四张表完成，现升 v2（加 likes、去 level、去 openid、时区） |
| 2 | 句子种子数据（27 句） | 库里 27 条 | ✅ 完成 | 2026-08-19 | 全部保留不去重 |

### 第二批：后端 API（用户 + 挑战赛主链路）

| # | 任务 | 验证 | 状态 | 完成时间 | 备注 |
|---|---|---|---|---|---|
| 3 | 句子接口 `GET /api/sentences?n=` | curl 返回句子 | ✅ 完成 | 2026-08-19 | `{"sentences":[...]}`，默认 5，上限 50，5 单测 |
| 4 | 用户系统：注册/登录 `POST /api/auth/*` + 鉴权 | curl 返回 token | ⬜ 待办 | — | 邮箱+密码，bcrypt |
| 5 | 评分服务（识别↔参考文本相似度） | 单测通过 | ⬜ 待办 | — | 及格线 60 |
| 6 | 评分接口 `POST /api/score`（需登录） | curl 返回分数 | ⬜ 待办 | — | 整数分，无等级，存录音+分数 |
| 7 | 正确数排行榜 `GET /api/leaderboard/correct` | curl 返回排名 | ⬜ 待办 | — | `score>=60` 计数 |

### 第三批：后端 API（点赞）

| # | 任务 | 验证 | 状态 | 完成时间 | 备注 |
|---|---|---|---|---|---|
| 8 | 点赞接口 `POST/DELETE /api/recordings/{id}/like` | curl 幂等 | ⬜ 待办 | — | 需登录 |
| 9 | 点赞数榜 `GET /api/sentences/{id}/recordings` | curl 返回按赞排名 | ⬜ 待办 | — | 别人录别人听 |

### 第四批：前端页面

| # | 任务 | 验证 | 状态 | 完成时间 | 备注 |
|---|---|---|---|---|---|
| 10 | 页面路由 + 统一布局（补登录页） | build 通过 | 🟡 部分 | — | 6 页面已建，缺 login |
| 11 | 首页四功能入口卡片 | 页面渲染 | ⬜ 待办 | — | 挑战赛为主入口 |
| 12 | 登录/注册页 | 流程走通 | ⬜ 待办 | — | 新增，存 token |
| 13 | 挑战赛页（刷→录→评分→颜色档） | 流程走通 | ⬜ 待办 | — | 录音方案 A，颜色绿/黄/红 |
| 14 | 排行榜页（正确数榜 + 点赞榜） | 显示排名 | ⬜ 待办 | — | 点赞/听录音交互 |
| 15 | 转写页（录音/上传→文本） | 出文本 | ⬜ 待办 | — | 复用 /api/asr |

### 第五批：次优先 + 联调

| # | 任务 | 验证 | 状态 | 完成时间 | 备注 |
|---|---|---|---|---|---|
| 16 | TTS 页（文本→方言语音） | 游戏本验证 | ⬜ 待办 | — | CosyVoice2 |
| 17 | 互转页（方言↔普通话音频） | 游戏本验证 | ⬜ 待办 | — | |
| 18 | 前后端联调（含 webm 解码验证） | 端到端走通 | ⬜ 待办 | — | 触发调整项 #3 |
| 19 | 游戏本部署 | 全功能跑通 | ⬜ 待办 | — | 触发调整项 #2/#4/#5 |

## 前置任务（已完成，不计入 19 项）

| 项 | 状态 | 完成时间 |
|---|---|---|
| 后端骨架 + ASR 识别 + CORS | ✅ | 2026-08-19 |
| 前端骨架（Nuxt4 + Tailwind + proxy） | ✅ | 2026-08-19 |

## 依赖关系

```
第一批(数据层) → 第二批(用户+主链路) → 第三批(点赞) → 第四批(前端) → 第五批(联调/部署)
     │              │
     └── 评分/榜依赖句子表和录音表
                    ├── 点赞(8/9) 依赖用户系统(4)和录音(6)
                    └── TTS/互转(16/17) 可并行，游戏本验证
```

## 进度日志

- 2026-08-19 — 数据模型升 v2：五张表 + 邮箱登录 + 点赞 + 中国时区；任务清单重构为 19 项；新增 handoff 同步说明
- 2026-08-19 — #3 句子接口完成（`{"sentences":[...]}`，5 单测）
- 2026-08-19 — #2 句子种子导入完成（27 句，幂等；27 录音=7 句×3~4 说话人，按用户决策全部保留）
- 2026-08-19 — #1 SQLAlchemy 模型完成（四张表，后升 v2）
- 2026-08-19 — 后端 ASR 识别跑通 + CORS 完成（调整项 #1 关闭）

## 关联文档

- 项目设计：`2026-08-19-泰州方言通-项目设计.md`
- 数据模型：`数据模型.md`（v2）
- API 契约：`API契约.md`（v2）
- 模型集成规范：`2026-08-19-模型集成规范.md`
- 后端 review 调整项：`2026-08-19-后端review调整项.md`
- 同步说明：`handoff-2026-08-19-数据模型v2同步.md`
