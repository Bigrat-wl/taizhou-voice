# 2026-08-20 晚上 汇报日志

---

*00:48*

## README 部署教程更新

### 做了什么

根据部署踩坑经验，更新 README.md 三处：

1. **第5步启动前端命令修正**：`npm install` / `npm run dev` → `pnpm install` / `pnpm dev`（项目使用 pnpm）
2. **新增「初始化数据」步骤**：第6节，说明种子数据导入（27句 + 测试账号），注明删库重建后必做
3. **常见问题补充三条**：
   - 浏览器录音 webm 格式说明（后端自动 ffmpeg 转码）
   - Nuxt 4 代理使用 `nitro.devProxy`（非 `devServer.proxy`）
   - 删库重建后需重新 seed

### 改动文件

| 文件 | 变更 |
|------|------|
| `README.md` | 第5步 npm→pnpm；新增第6节「初始化数据」；常见问题+3条 |

### 验证结果

- Markdown 格式确认：代码块、引用、列表渲染正常
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

无。

---

*00:55*

## webm 解码问题修复（调整项 #3）

### 做了什么

浏览器录音产出 webm/opus 格式，libsndfile（soundfile 底层）不支持解码，导致 `Format not recognised` 错误。修复方案：在 ASR 识别前用系统 ffmpeg 把上传音频统一转为 16kHz 单声道 wav。

具体改动：

1. **`backend/app/main.py`**：新增 `_ensure_wav()` 函数 + 在 `asr()` 端点中调用，webm/ogg/m4a/aac/wma 等格式先走 ffmpeg 转码再识别；ffmpeg 失败返回 422 + 明确错误信息；新增临时 wav 文件清理逻辑
2. **`backend/app/routers/score.py`**：同步新增 `_ensure_wav()` + 在 `score()` 端点中调用；`_convert_to_wav()` 改用 ffmpeg 替代 librosa（不再依赖 soundfile 解码 webm）；新增 `subprocess`/`shutil` 导入
3. **`backend/tests/test_asr_webm.py`**（新增）：3 个测试覆盖 `/api/asr` 的 webm 转码路径
4. **`backend/tests/test_score.py`**：新增 `TestScoreWebmTranscode` 类，3 个测试覆盖 `/api/score` 的 webm 转码路径

### 改动文件

| 文件 | 变更 |
|------|------|
| `backend/app/main.py` | 新增 `_ensure_wav()`、`_FFMPEG_ONLY_SUFFIXES`；`asr()` 加 ffmpeg 转码 + wav 临时文件清理 + RuntimeError 处理 |
| `backend/app/routers/score.py` | 新增 `_ensure_wav()`、`_FFMPEG_ONLY_SUFFIXES`；`score()` 加 ffmpeg 转码 + wav 临时文件清理 + RuntimeError 处理；`_convert_to_wav()` 改用 ffmpeg |
| `backend/tests/test_asr_webm.py` | 新增：webm 转码成功/失败/wav 跳过 3 个测试 |
| `backend/tests/test_score.py` | 新增 `TestScoreWebmTranscode` 类 3 个测试 + `MagicMock` 导入 |

### 验证结果

- `cd backend && UV_CACHE_DIR=$PWD/.uv-cache uv run pytest` → **70 passed, 0 failed**（含 6 个新增 webm 测试）
- ffmpeg 实测：`ffmpeg -y -i test.webm -ar 16000 -ac 1 test.wav` → 转码成功，输出合法 wav
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

无。调整项 #3 状态更新为已完成。

---

*01:00*

## 挑战页「评分中」提示消失 bug 修复

### 做了什么

提交评分按钮的 `v-if` 条件只判断了 `phase === 'recorded'`，点击提交后 phase 变为 `submitting`，按钮瞬间消失，导致用户看不到「评分中…」+ 旋转图标。

修复：将条件从 `phase === 'recorded'` 改为 `phase === 'recorded' || phase === 'submitting'`，使按钮在评分请求期间保持显示。

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/pages/challenge.vue` | 第 370 行提交按钮 `v-if` 条件增加 `phase === 'submitting'` 分支 |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

无。

---

*01:06*

## 用户区下拉菜单改造

### 做了什么

改造已登录用户区交互：将原来的「用户昵称 + 退出按钮」平铺布局，改为「用户头像 + 昵称 + 下拉箭头」按钮，点击后弹出下拉菜单，菜单内含「退出登录」选项。

具体改动：

1. **新增状态管理**：`dropdownOpen` 控制菜单开关，`dropdownRef` 引用菜单容器 DOM
2. **切换逻辑**：`toggleDropdown()` 切换菜单显隐，`closeDropdown()` 关闭菜单
3. **点击外部关闭**：`onMounted`/`onUnmounted` 注册/注销 `document` 级 `click` 事件监听，判断点击目标是否在 `dropdownRef` 内部
4. **退出登录优化**：`handleLogout()` 先关闭菜单再执行 `logout()` + 路由跳转
5. **样式**：复用 lucide 图标（`user` / `chevron-down` / `log-out`），Tailwind 样式与导航栏按钮一致（`rounded-lg`、`hover:bg-slate-100`、`text-sm font-medium` 等）；下拉菜单带 `border`、`shadow-lg`、`bg-white`，右对齐（`right-0`）
6. **动画**：Vue `<Transition>` 组件实现 scale + opacity 进出动画
7. **未登录**：保持原「登录」按钮不变（`NuxtLink` → `/login`）

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/layouts/default.vue` | 新增 `dropdownOpen`/`dropdownRef` 状态、`toggleDropdown`/`closeDropdown`/`onClickOutside` 方法、`onMounted`/`onUnmounted` 生命周期；模板改为按钮触发 + `<Transition>` 包裹下拉菜单；新增 `lucide:chevron-down` 图标 |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误，无组件解析警告
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

无。

---

*03:10*

## TTS 语音合成功能实现（任务 #16）

### 做了什么

实现 TTS 语音合成功能三部分，严格按 API 契约 §1.8 和数据模型 §4 实现：

1. **后端 services/tts_service.py**（新增）：CosyVoice2 语音合成服务封装
   - 懒加载：`is_loaded` 属性判断，`_ensure_loaded()` 触发加载
   - GPU 守卫：`is_gpu_available` 检查 `torch.cuda.is_available()`，无 GPU 时 `load()` 抛 `RuntimeError`
   - 多文件加载：检查 `llm.pt` / `flow.pt` / `hift.pt` 三个模型文件
   - 模型目录：默认 `/home/rat/dialect_asr_system/tts/output/final`，可通过环境变量 `TTS_MODEL_DIR` / `TTS_DEVICE` 配置
   - 线程安全：`_load_lock` + `_infer_lock`，推理串行执行
   - `synthesize(text)` → `(waveform, sample_rate)`：文本→音频波形

2. **后端 routers/tts.py**（新增）：`POST /api/tts` 端点
   - 请求体：`{"text": "今朝天气老好"}`（Pydantic 校验：必填，≤200 字）
   - 响应 200：`{"audio_url": "/data/audio/tts_xxx.wav"}`
   - 响应 503：模型未加载（GPU 不可用等）
   - 响应 400：文本为空/超长；响应 500：合成失败
   - 音频落盘：`backend/data/audio/tts_{ts}.wav`

3. **前端 pages/tts.vue**（重写）：完整 TTS 页面
   - 文本输入框（textarea），实时字数统计 + 超限提示
   - 「合成语音」按钮 + spinner；合成成功后显示 `<audio>` 播放器
   - 复用 `getErrorDetail` 处理后端错误；绿色主题（`emerald-*`）

4. **后端 main.py**（修改）：导入 TTS 服务 + router 注册 + health 增加 `tts_loaded` + lifespan 退出卸载

5. **测试 tests/test_tts.py**（新增）：17 个测试全 mock
   - 模型未加载 503、文本校验 400/422、成功合成 200 + 落盘、合成异常 500、服务层单元测试

### 改动文件

| 文件 | 变更 |
|------|------|
| `backend/app/services/tts_service.py` | 新增：CosyVoice2Service 类 |
| `backend/app/routers/tts.py` | 新增：POST /api/tts 端点 |
| `backend/app/main.py` | TTS 服务初始化 + router 注册 + health 字段 |
| `frontend/app/pages/tts.vue` | 重写：完整 TTS 页面 |
| `backend/tests/test_tts.py` | 新增：17 个 TTS 测试 |

### 验证结果

- `cd backend && UV_CACHE_DIR=$PWD/.uv-cache uv run pytest` → **87 passed, 0 failed**（含 17 个新增 TTS 测试）
- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

- CosyVoice2 实际推理需要在游戏本（GPU 环境）上验证，开发机只写代码 + mock 测试
- `inference_sft` 的具体调用方式（spk_id 参数、返回格式）可能需要根据实际 CosyVoice2 版本微调
- TTS 音频输出采样率暂定 22050（CosyVoice2 默认），实际可能需要调整为 16kHz 统一格式

---

*18:21*

## 实时翻译页重写（任务 #16 / #17 合并）

### 做了什么

将原 TTS 页面（`/tts`）+ 音频互转页面（`/convert`）合并为「实时翻译」页面（`/translate`），替代原有两个独立页面。

核心功能：

1. **上区：左右并排录音按钮**
   - 左侧「泰州话」（amber 色系）、右侧「普通话」（sky 色系）
   - 各自独立 `useRecorder` 实例，互不干扰
   - 录完后显示文件大小 + 试听回放（`useBlobUrl`）+「识别 → 合成」按钮
   - 禁用逻辑：录音中 / 处理中时禁用另一侧按钮

2. **下区：转写结果时间线**
   - 按时间倒序展示每条翻译记录
   - 泰州话句子：amber 色标题「泰州话」+ 时间 + 原文 + 绿色译文
   - 普通话语句：sky 色标题「普通话」+ 时间 + 原文 + 绿色译文
   - 每条右侧播放按钮：点击后调用 `POST /api/tts` 合成译文语音并播放（互转播放）
   - 合成结果缓存：已合成的译文不再重复请求

3. **组件拆分**
   - `components/translate/TranslateTimelineItem.vue`：单条时间线条目（props: direction/source/target/time）
   - `pages/translate.vue`：页面容器 + 录音逻辑 + 识别/合成流程

4. **导航更新**
   - `useNav.ts`：移除 TTS 和音频互转两个导航项，新增「实时翻译」（`lucide:languages`）
   - `index.vue`：首页入口卡片从 3 列改为 2 列（转写 + 实时翻译），移除旧 TTS / 互转卡片

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/pages/translate.vue` | 新增：实时翻译页面 |
| `frontend/app/components/translate/TranslateTimelineItem.vue` | 新增：时间线条目组件 |
| `frontend/app/pages/tts.vue` | 删除 |
| `frontend/app/pages/convert.vue` | 删除 |
| `frontend/app/composables/useNav.ts` | TTS + 互转 → 实时翻译 |
| `frontend/app/pages/index.vue` | 首页卡片 3→2 列 + 路由更新 |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误，无组件解析警告
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

- 互转播放逻辑依赖后端 `POST /api/tts` 已实现（CosyVoice2），实际效果需在 GPU 环境验证
- 当前译文文本与原文相同（ASR 识别后直接用于 TTS 合成），真正的方言↔普通话翻译需额外 NMT 模型，演示期可接受

---

*18:35*

## 实时翻译页重写 v2（ASR 翻译 + TTS 播放）

### 做了什么

修正上一版（18:21）的实现问题，重写 `translate.vue` 和 `TranslateTimelineItem.vue`，使流程符合任务要求：

**核心修正：**

1. **TTS 合成时机前移**：从「点击播放时懒合成」改为「提交时预合成」。流程变为：录完 → 点「识别翻译」→ POST /api/asr 识别 → 显示普通话文字 → POST /api/tts 合成方言译文 → 加入时间线（含预合成 audioUrl）
2. **时间线条目简化**：`TranslateTimelineItem` 不再做 TTS 请求，只接收 `audioUrl` prop 直接播放；移除 `loadingAudio` 状态和 `$fetch` 调用
3. **数据结构修正**：`TranslateEntry` 接口从 `{ direction, source, target, time }` 改为 `{ direction, text, audioUrl, time }`，移除冗余的 `target` 字段
4. **按钮文案**：从「识别 → 合成」改为「识别翻译」，更符合任务描述
5. **移除未使用的 `useAuth` 导入**

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/pages/translate.vue` | 重写：TTS 合成前移到提交流程；移除 useAuth 导入；按钮文案改为「识别翻译」 |
| `frontend/app/components/translate/TranslateTimelineItem.vue` | 重写：移除懒合成逻辑，改为播放预合成 audioUrl；接口字段 direction/text/audioUrl/time |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

- 同上一版：实际 ASR/TTS 效果需 GPU 环境验证

---

*18:41*

## 实时翻译页 v3（TTS 失败容错）

### 做了什么

修正 v2 版本的健壮性问题：当 TTS 合成失败时（如 GPU 不可用、模型未加载），原实现会丢弃 ASR 识别结果，用户看不到任何输出。

**核心修正：**

1. **TTS 失败不阻塞**：将 TTS 调用包裹在独立 try-catch 中，失败时记录 `ttsError` 但仍将识别文字加入时间线
2. **TranslateEntry 接口扩展**：`audioUrl` 改为 `string | null`，新增可选字段 `ttsError?: string | null`
3. **TranslateTimelineItem 适配**：
   - 播放按钮和 `<audio>` 元素仅在 `audioUrl` 非空时渲染（`v-if="audioUrl"`）
   - TTS 失败时在识别文字下方显示红色错误提示（`lucide:alert-circle` 图标）
4. **模板透传 `ttsError`**：`translate.vue` 模板中将 `entry.ttsError` 传给条目组件

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/pages/translate.vue` | submit 函数：TTS 独立 try-catch，失败仍入时间线；模板透传 ttsError |
| `frontend/app/components/translate/TranslateTimelineItem.vue` | 接口 audioUrl 改为可选；新增 ttsError prop；播放按钮/音频 v-if 保护；TTS 错误提示 |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

- 同上一版：实际 ASR/TTS 效果需 GPU 环境验证

---

*18:44*

## 实时翻译页 v4（细粒度加载状态）

### 做了什么

将原来的单一 `processing` 布尔值拆分为三阶段状态机 `phase: 'idle' | 'recognizing' | 'synthesizing'`，实现更精细的加载反馈：

**状态流转：**
```
idle → (点提交) → recognizing → (ASR 完成) → synthesizing → (TTS 完成) → idle
                                                          ↘ (TTS 失败) → idle（仍入时间线）
```

**UI 反馈：**
- **录音中**：录音按钮变红 + 显示秒数（已有，不变）
- **识别中**：提交按钮显示「识别中…」+ 旋转图标，全部按钮禁用
- **合成中**：提交按钮切换为「合成中…」+ 旋转图标，全部按钮禁用
- **完成**：加入时间线，按钮恢复

**关键改动：**
1. `processing: shallowRef(false)` → `phase: shallowRef<'idle' | 'recognizing' | 'synthesizing'>('idle')`
2. 新增计算属性 `isProcessing = phase !== 'idle'`、`submitLabel`（根据 phase 返回文案）
3. `submit` 函数内 ASR 前设 `phase = 'recognizing'`，ASR 后设 `phase = 'synthesizing'`
4. 录音按钮 `:disabled` 条件使用 `idle`（即 `!anyRecording && !isProcessing`）
5. 提交按钮 `:disabled` 条件使用 `isProcessing`，文案使用 `submitLabel`

### 改动文件

| 文件 | 变更 |
|------|------|
| `frontend/app/pages/translate.vue` | `processing` → `phase` 三阶段；新增 `isProcessing`/`submitLabel`；submit 函数分阶段设置 phase；模板按钮文案/禁用逻辑更新 |

### 验证结果

- `cd frontend && pnpm build` → **构建成功**，无 TS 类型错误
- 未启动 dev server（按规则不启停开发服务器）

### 遗留问题

- 同上一版：实际 ASR/TTS 效果需 GPU 环境验证
